package Java8;

public class ipl {

}
/*	for(Delivery delivery : Deliveries) {

if(  match.getMatchId().equals(delivery.getMatchId()) ) {
	
	String yearF = "YEAR= " + match.getSeason() + ",TEAM_NAME= " + delivery.getBattingTeam() + " FOUR ";
	
	if( delivery.getBatsmanRun().equals("4") ) {
		
		flag = data.containsKey(yearF) ? (data.put(yearF, (Integer) data.get(yearF)+1)) : data.put(yearF, 1);
	}
	
	String yearS = " YEAR= " + match.getSeason() + ", TEAM_NAME= " + delivery.getBattingTeam() + " SIX ";
	
	if( delivery.getBatsmanRun().equals("6") ) {
		
		flag = data.containsKey(yearS) ? ( data.put(yearS, (Integer) data.get(yearS)+1)) : data.put(yearS, 1);
	}
	
	String yearT = "][YEAR= " + match.getSeason() + ",TEAM_NAME= " + delivery.getBattingTeam() + " TOTAL_RUN ";
	
	int b = Integer.parseInt( delivery.getTotalRun() );
	flag = data.containsKey(yearT) ? ( data.put(yearT, (Integer) data.get(yearT)+ b) ) : data.put( yearT, b );
}
}*/
